{
  "name": "website",
  "version": "1.0.0",
  "description": "Personal website / portfolio of Michael Levy.",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-ghpages": "npx @11ty/eleventy --pathprefix=/michaelevy.github.io/",
    "compose": "docker-compose down && docker-compose up -d --build",
    "compose-web": "docker-compose up -d --build website",
    "compose-api": "docker-compose up -d --build backend"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-img": "^6.0.4",
    "@11ty/eleventy-navigation": "^1.0.4",
    "arrify": "^3.0.0"
  }
}
