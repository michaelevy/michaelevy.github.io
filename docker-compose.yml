version: '3.8'

services:
  website:
    build: .
    container_name: michaelevy-website
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - web

networks:
  web:
    driver: bridge
