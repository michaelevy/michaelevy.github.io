version: '3.8'

services:
  website:
    build: .
    container_name: michaelevy-website
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - web

networks:
  web:
    driver: bridge
